requirements:
{
    logiciel : john-the-reaper
    savoir utiliser find
    savoir ce que c'est race condition
    Utilisation de $() et ``
    Utilisation de regex
    Utilisation de gdb
}

Premier niveau: ssh level00@192.168.1.34 -p 4242 pwd : level00
get all users list = cat /etc/passwd

find / -name level00 2>&1 | grep -v "Permission denied"
find / -user flag00 2>&1 | grep -v "Permission denied"

flag01 = flag01:42hDRfypTqqnw:3001:3001::/home/flag/flag01:/bin/bash

